# Installeer het iotroothelper package op Raspberry Pi (alleen nodig voor GPIO zonder sudo)
sudo apt-get update
sudo apt-get install iotroothelper
sudo usermod -a -G gpio $USER
# Log opnieuw in om de groepswijzigingen toe te passen

aym QR libraries used/installed:
